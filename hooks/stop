#!/usr/bin/env bash
# Stop hook for superpowers plugin (Plan Overlap Guard)

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
JS_SCRIPT="${SCRIPT_DIR}/logic/plan_drift_check.js"

if [ -f "${JS_SCRIPT}" ]; then
    # Pass stdin to the node script
    node "${JS_SCRIPT}"
fi
