#!/usr/bin/env bash
# PreToolUse hook for superpowers plugin (Guard Commit)

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
JS_SCRIPT="${SCRIPT_DIR}/logic/guard_commit.js"

if [ -f "${JS_SCRIPT}" ]; then
    # Pass stdin to the node script
    node "${JS_SCRIPT}"
fi
