#!/usr/bin/env bash
# Stop hook for superpowers plugin (Unit Test Enforcement)

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
JS_SCRIPT="${SCRIPT_DIR}/logic/run_tests_before_commit.js"

if [ -f "${JS_SCRIPT}" ]; then
    # Pass stdin to the node script
    node "${JS_SCRIPT}"
fi
